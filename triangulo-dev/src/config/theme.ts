import { theme } from "@chakra-ui/core";

export default {
  ...theme,
  fonts: {
    ...theme.fonts,
    body: "'Inter', system-ui, sans-serif",
    heading: "'Inter', system-ui, sans-serif",
  },
};
